export enum TransactionStatus {
  Authorization = 'counterparties.progressBar.authorization',
  Verification = 'counterparties.progressBar.verification',
  Payment = 'counterparties.progressBar.payment',
  Status = 'counterparties.progressBar.status',
}